import time
is_prime = True
n = int(input("Enter number: \n"))
start=time.time()
if n <= 1 :
    is_prime = False
else:
    for number in range(2, n):
        if n%number == 0:
            is_prime = False

print(is_prime)
end= time.time()
print(end-start)
